[Unit]
Description=PiBot Telegram Bot
After=network.target

[Service]
Type=simple
User=pibot
WorkingDirectory=/opt/pibot
ExecStart=/opt/pibot/.venv/bin/python source/pibot.py
Restart=always
RestartSec=10
Environment="TELEGRAM_TOKEN=your_token"

[Install]
WantedBy=multi-user.target
